version: "3.7"

services:
    wireguard-server:
        build: .
        volumes:
            - ./server.conf:/wireguard.conf
            - ./entrypoint_server.sh:/entrypoint_routing.sh
        cap_add:
            - NET_ADMIN
        restart: unless-stopped
        networks:
            - wireguard-network
        devices:
            - /dev/net/tun:/dev/net/tun

    iperf3-server:
        image: networkstatic/iperf3
        command: -s
        depends_on:
            - wireguard-server
        networks:
            - wireguard-network


    wireguard-client:
        build: .
        volumes:
            - ./client.conf:/wireguard.conf
            - ./entrypoint_client.sh:/entrypoint_routing.sh
        cap_add:
            - NET_ADMIN
        restart: unless-stopped
        networks:
            - wireguard-network
        devices:
            - /dev/net/tun:/dev/net/tun

    wireguard-client-iperf3:
        image: networkstatic/iperf3
        command: -c iperf3-server --bidir --parallel 128 -t 120
        network_mode: "service:wireguard-client"
        depends_on:
            - wireguard-client
            - iperf3-server


networks:
    wireguard-network:
        driver: bridge